# Project Context

## Project Summary
- Purpose: Build an evidence-gated BREAK100 research and MT5 trading system that fails closed when edge, safety, or approval evidence is insufficient.
- Main technologies: Planned Python 3, MQL5/MT5, ONNX, Parquet, and SQLite or DuckDB; project compatibility is not yet verified.
- Primary entry points: `break100.nontrading.ModeController` is the first executable contract; architecture and PMP controls are under `docs/`.

## Current State
- Current version: MQL5 EA v2.33 (detects/draws/logs only, places no orders in any mode) plus `mql5/Scripts/RES-SUP OCO.mq5` v1.01, which is the sole order path and **is enabled for live/real accounts** (owner-authorised, DECISION_LOG D-007); Python side adds an offline research package alongside the earlier safety kernel.
- Working features: PMP/architecture controls, broker-independent mode controller, after-cost SafeEV abstention, stop-risk sizing, hard loss/drawdown gates, the BREAK100 Box Trading EA (detection, drawing, Telegram and Shadow ledger only — it places no orders as of v2.33), and an offline research package (`src/break100/research/`) that ports the MQL5 box detector to Python, replays fills tick-by-tick, and enforces chronological/trial-count-deflated significance testing.
- Recent verified changes: Full Python suite passed 91 tests on 2026-09-02 (up from 36), `mypy` now clean on 16 source files, `ruff` clean after an import-order autofix; MQL5 EA reached v2.23 (box-size trade filter, gapped-entry stop re-anchor, gated Live toggle, always-on Shadow ledger, v2 capture schema). A 20-configuration tick-level backtest over the full 41.2-day/3.56M-tick broker history found **no edge**: the shipped baseline nets −4.9% ROI (M30) with the measured 46.7% win rate matching the random-walk prediction from spread geometry (46.25%) almost exactly; the best of 20 variants reached t=+0.12, far short of the ~2.45 needed to clear the trial-count penalty. A quarantined v1 dataset (`research/quarantine/`) was found corrupted (MAE mirrored MFE, so it was never an independent measurement) and retired in favor of a `*_v2_*` schema.
- Known issues: No data/event/model pipeline, audit store, execution adapter, datasets, Shadow/Demo evidence, approved account, or owner control lease beyond the research findings above; broker history is capped at 41.2 days (1,979 M30 bars / 131 box events), well short of the ~2,000-event target, so any result is powered to detect only a large effect. The v2.24-2.31 EA work is now committed and pushed to `feat/validated-signal` (the earlier "on disk but not in git" note is resolved).
- Pending work: Decide whether to keep collecting forward capture toward a usable M15-variant sample (~8.5 events/day) or pursue payoff-asymmetry research instead of direction prediction (direction is confirmed ~50/50 in every variant); complete G1 config/data/audit schemas; build the causal tick/event pipeline using TDD.

## Architecture
- Main components: Implemented non-trading mode, decision, and risk contracts; a live MQL5 EA (box detect/arm/fill, Shadow ledger, capture, Telegram, ML/RL learner); an offline research package (backtest, box detector port, bar-level replay, chronological holdout/significance testing, per-box policy, structure/tradeability battery, walk-forward metrics); planned data, event, model, isolated Demo/Live execution, and Indicator layers.
- Important directories: `src/break100/nontrading`, `src/break100/decision`, `src/break100/risk`, `src/break100/research`, `mql5/Experts`, `mql5/Include/Break100`, `tools`, `research` (backtest/data-ceiling findings, quarantined v1 data), `tests`, and `docs`.
- Data flow: Live path — MT5 ticks to `Box.mqh` detection to arm/fill/OCO to Shadow ledger and `train.csv` capture to HF sync/learner. Research path — exported tick/bar history (`tools/export_history.py`) to the ported box detector (`boxdetect.py`) to bar-level replay (`replay.py`) or tick-level backtest (`backtest.py`) to chronological holdout with trial-count-deflated significance (`holdout.py`). Planned: tick to causal event to feature snapshot to frozen model to SafeEV and gates to mode adapter, ledger, audit, and reconciliation.
- External dependencies: Local Python 3.14.3 and two MetaEditor/MT5 installations are discovered and now actively used (`tools/export_history.py`, `tools/screen_symbols.py` pull read-only from a running MT5 terminal via the MetaTrader5 Python bridge); broker retains only 41.2 days of BREAK100 history (a rolling window, not a one-time limit) so Demo access remains the gap for live evidence.

## Constraints
- Compatibility requirements: Preserve causal UTC bid/ask processing and verify Python, ONNX, and MQL5 parity before promotion.
- Security requirements: the EA reaches no broker order path in any mode (v2.33). The single order path is `mql5/Scripts/RES-SUP OCO.mq5`, which as of v1.01 is **live-enabled by default with no account allowlist**, by explicit owner decision D-007 — a recorded, deliberate departure from the original allowlist/lease requirement. Its remaining guards are manual launch, a per-run input dialog, and a live-run announcement in the log.
- Files or logic that must not be changed: Never weaken gate criteria, risk ceilings, evidence requirements, or fail-closed defaults without explicit owner authority and recorded decision.

## Loop History

### 2026-08-13 — Gate G0 Initiation Baseline
- Request: Begin the full BREAK100 adaptive AI/ML/RL MT5 project with Gate G0 and PMP controls before trading behaviour.
- Changes: Added project charter, scope, WBS, gates, RTM, RAID, quality/test/implementation plans, logs, architecture/data specifications, current-state report, order-path audit, and draft handover.
- Files affected: `README.md`, `CONTEXT.md`, and documentation under `docs/`.
- Validation: Required-artifact/H1 scan passed for 18 pre-context artifacts; secret-pattern scan passed; no source/order path exists.
- Result: G0 passed after artifact validation; G1 remains in progress; Shadow, Demo, Live Canary, and Live are NO-GO.
- Remaining work: Add executable mode/state contracts and structural non-trading dependency tests before any broker adapter.

### 2026-08-13 — G1 Broker-Independent Mode Contract
- Request: Begin executable safety scaffolding without adding a broker-order path.
- Changes: Added Python packaging, explicit operating/account/system states, sequential gate promotion, Demo and Live control checks, continuous lease expiry, fail-closed fault handling, and AST dependency-boundary tests.
- Files affected: `pyproject.toml`, `.gitignore`, `.agents/specs`, `src/break100/nontrading`, `tests`, and relevant project-control documents.
- Validation: Wheel build and compile passed; `python -m pytest -q` passed 13 tests; line-length and diff checks passed; `ruff` and `mypy` skipped as unavailable.
- Result: Partial G1 safety contract verified; no order-submission source exists; all modes beyond Observe remain NO-GO.
- Remaining work: Define config/data/audit/risk interfaces and implement the decision/risk safety kernel before any execution adapter.

### 2026-08-14 — G1 Decision and Risk Safety Kernel
- Request: Enforce positive conservative after-cost expectancy and stop-risk sizing before any execution work.
- Changes: Added validated outcome/cost inputs, uncertainty-adjusted SafeEV, mandatory NO_TRADE, exact Decimal sizing, broker-minimum-anchored volume steps, configurable limits beneath hard ceilings, and loss/drawdown/position gates.
- Files affected: `src/break100/decision`, `src/break100/risk`, tests, behavioral spec, RTM, gate report, and change log.
- Validation: Wheel/compile checks passed during the work package; full `python -m pytest -q` passed 36 tests; line-length and diff checks passed; `ruff` and `mypy` remained unavailable.
- Result: Deterministic decision/risk contracts verified; no profitability or execution evidence exists; all modes beyond Observe remain NO-GO.
- Remaining work: Add versioned config, tick/event/audit schemas, causal replay, and statistical edge validation.

### 2026-09-02 — MQL5 EA to v2.23 and offline research package
- Request: Ship the live BREAK100 Box Trading EA through v2.15–2.23 (box-size filter, gapped-entry re-anchor, gated Live toggle, always-on Shadow ledger) and answer, with real broker history, whether the shipped strategy has after-cost edge.
- Changes: MQL5 EA/Include tree updated through v2.23 (see `docs/project-management/CHANGE_LOG.md`); added `src/break100/research/` (line-for-line Python port of the box detector, tick-level backtest engine, bar-level replay, chronological holdout with trial-count-deflated significance, per-box policy module replacing the collapsed HF logistic model, tradeability structure battery, walk-forward metrics) plus `tools/export_history.py`, `tools/screen_symbols.py`, `tools/backtest_break100.py`, and matching tests; quarantined a corrupted v1 dataset (`research/quarantine/`, MAE mirrored MFE) and moved to a `*_v2_*` capture schema.
- Files affected: `mql5/Experts/Break100 Box Trading.mq5`, `mql5/Include/Break100/*`, `src/break100/research/*`, `tools/*`, `tests/test_{boxdetect,holdout,policy,replay,structure}.py`, `research/BACKTEST_RESULTS.md`, `research/DATA_CEILING.md`, `research/quarantine/README.md`.
- Validation: `python -m pytest -q` passed 91 tests; `python -m mypy src` clean; `python -m ruff check` clean after an import-order autofix; 20-configuration backtest run against 3,559,792 ticks (41.2 days, complete 1 Hz record).
- Result: No statistically significant edge found in any of 20 tested configurations after trial-count correction (best t=+0.12 vs ~2.45 needed); the measured 46.7% win rate is explained entirely by spread geometry on a driftless walk, not by the strategy. Direction is confirmed ~50/50, so edge — if any exists — must come from payoff asymmetry, not direction prediction. All modes beyond Observe/Shadow remain NO-GO; this is a research finding, not a go/no-go gate change.
- Remaining work: This large change set is on disk and passing tests but not yet staged/committed to git. Decide research direction (forward-capture toward the M15 variant vs. payoff-asymmetry analysis) before further backtest iteration.

### 2026-09-03 — EA v2.24-2.31: two-leg OCO, LIVE-init fix, Telegram/dashboard audit
- Request: Add a runner leg to the OCO pair, log manual trades into the Shadow ledger, allow one-click chart entry, and audit the alerting path. Reconstructed for this entry from `docs/project-management/CHANGE_LOG.md` and the `#property description` history in the EA, after the originating session's transcript was lost.
- Changes: Two-leg OCO per side (leg 1 exits at TP1, leg 2 runs to TP3) behind `InpTwoLegs`, fixed volume per leg via `InpLotsPerLeg`; manual-trade shadow logging; one-click chart entry; on-chart dashboard mirroring the Telegram alert (v2.27) with a spacing fix that had pushed the LIVE/ENTER buttons off-screen (v2.28); history-box repaint fixed to key off `n_boxes` rather than the saturating `hist_n` (v2.29); Telegram audit for duplicate TP/close alerts, runner-blind sibling cancels, and a self-test version string hardcoded to "v2.19" that had survived ten releases (v2.30); orphaned BUY runner pending cleaned up when SELL leg-1 placement fails (v2.31).
- LIVE-init bug: a leftover account-type check from v1.42-v1.61, predating the LIVE gate, was silently forcing OBSERVE after the four-gate check had already passed - so LIVE was structurally unreachable even when correctly armed. Fixed; the four gates (`InpAllowLiveTrading` + real account + login allowlist + armed chart button) remain the only path, arming is still never persisted, and the default is still off.
- Files affected: `mql5/Experts/Break100 Box Trading.mq5` (now 3,373 lines, `B100_VERSION` 2.31), `mql5/Include/Break100/*`, `docs/project-management/CHANGE_LOG.md`.
- Validation: MQL5 side is owner-verified on chart; no Python source changed, so the 91-test suite from 2026-09-02 still stands as last recorded. Not re-run in the recovery session - this container has no `pytest` installed.
- Result: Code is committed and pushed to `feat/validated-signal` at `05e3d90`; nothing from the lost session was lost from git. The no-edge research finding from 2026-09-02 is unchanged and unchallenged by this work - v2.24-2.31 is execution and alerting plumbing, not an edge claim. All modes beyond Observe/Shadow/Demo_Auto remain NO-GO.
- Remaining work: Unchanged from 2026-09-02 - decide between forward capture toward a usable M15 sample and payoff-asymmetry research. Live trading data (`BREAK100_*_v2_*.csv`, shadow ledger, tick/bar capture) lives only in MT5's `Common\Files` on the owner's machine and is not in git by design; it must be pulled locally with `tools/export_history.py`.

### 2026-09-03 — Capture audit, unreachable order path, tick-path learning

- Request: ensure all data is actually being captured, and learn from the tick and the movement rather than only from box outcomes; then fix the on-chart dashboard, which was overprinting itself.
- Changes: **`B100ArmBoxOco` could never place an order.** An unbraced `if` made its `return` unconditional, so since v2.23 execution exited before the box-size gate, the learned direction gate and every `B100DemoPlacePending` call, and the Shadow ledger's `exec_decision` never advanced past the early tags — the counterfactual column the research programme depends on was therefore wrong, not merely absent. Four further sites of the identical shape sat below it and would each have re-blocked the path in turn; all five braced. Bar catch-up (`Capture.mqh`) walked newest-first against a monotonic `last_bar_time`, so after any stall or restart exactly one bar was written and every older bar in the gap was discarded — now walks oldest-first with `ArraySetAsSeries(r, false)` making the order explicit. On the research side, `Trade` now carries MFE/MAE in R, time to each extreme and entry slippage against the resting stop price; a new `asymmetry()` battery reports edge ratio, capture, payoff and tail ratios and winner/loser excursions apart. HUD: empty rows wrote an empty label, which the terminal renders as its default caption "Label" — the stray mint text and the overprinted box row in the owner's chart snapshot, and the reason two earlier attempts to delete an object *named* "Label" had no effect.
- Files affected: `mql5/Experts/Break100 Box Trading.mq5`, `mql5/Include/Break100/Capture.mqh`, `src/break100/research/backtest.py`, `tests/test_backtest.py`, `docs/project-management/CHANGE_LOG.md`, `CONTEXT.md`.
- Validation: `python -m pytest -q` passed 107 tests (91 before; 16 new, the first over the tick engine, which had none). `python -m mypy` clean on 16 source files; `python -m ruff check src/ tests/` clean. MQL5 changes are **not compile-verified** — no MetaEditor in this environment — but braces balance and a written detector reports zero remaining unbraced-body sites across the MQL5 tree.
- Result: the order path and the Shadow decision column are restored; bar capture no longer drops gaps; the payoff-asymmetry question the 2026-09-02 research reduced to is now measurable. No edge claim changes — v2.32 is correctness and instrumentation, and Live stays gated off.
- Remaining work: audit findings not yet actioned — `BREAK100_train_*.csv` carries a 43-field header in code against 41 fields on any pre-existing file, and `tools/break100_hf_sync.py` rewrites that file with `extrasaction="ignore"`, silently stripping `bars_to_mfe`/`bars_to_mae`; a second EA instance on the same symbol fails `FileOpen` silently and captures nothing; `B100_FAULT` never clears, so one fault ends event capture for the session; ticks are written on the server clock under a `utc_ms` header beside GMT-converted bars; `tools/export_history.py` truncates on re-export, so the archive can never outgrow the broker's rolling 41.2-day window; and `setup_v2`/`outcome_v2` are written headerless and read by nothing.

### 2026-09-03 — OCO execution moved to a standalone script (v2.33)

- Request: after v2.32 restored the EA's OCO path, the owner asked for a genuine MT5 **Script** (not an EA) to place the BUY STOP/SELL STOP pair off the chart's own RES/SUP levels, cancel the sibling on fill, and carry chart-derived SL/TP with a configurable lot size (default 0.01) — and asked that the equivalent placement logic be removed from the EA so the two could never both place an order for the same box.
- Changes: **`B100ArmBoxOco` no longer places orders.** The `if(B100BrokerOrderIntentPermitted(...)) { ... }` block that called `B100DemoPlacePending` (leg 1 and leg 2/runner, both sides) is deleted; the function still runs every pre-trade gate and keeps computing `g_levels` so the HUD, Shadow ledger and Telegram WATCH alert stay accurate, but the Shadow decision now tags `"SKIP_EXTERNAL_EXEC"` instead of a `TRADED`/`SKIP_MODE` split that no longer means what it says. `OnTradeTransaction`'s and `B100TelegramFill`'s sibling-cancel branches are left in place but permanently inert — their tracked tickets are never populated now — rather than stripped as a separate cleanup. New **`mql5/Scripts/RES-SUP OCO.mq5`**: reads RES/SUP from the EA's own rail objects (or manual price inputs), computes buy/sell stop, SL and TP with the same shape as `B100FillBoxLevels` (offset % of height, SL buffered beyond the opposite rail, TP at a configurable R-multiple, default 1R) — chart-derived, not arbitrary. Places both pendings under a new, distinct magic number (never the EA's `B100_MAGIC`, to rule out cross-reaction), then polls every 500ms inside `OnStart()` — a Script has no `OnTradeTransaction` — to cancel the sibling on a fill or both pendings on a configurable timeout (default 240 minutes). Lot size defaults to 0.01, plain input. Same demo-or-allowlisted-live account gate as the EA; running the script is itself the arming gesture, so it does not carry the EA's separate arm-button ceremony.
- Files affected: `mql5/Experts/Break100 Box Trading.mq5` (v2.33), `mql5/Scripts/RES-SUP OCO.mq5` (new), `README.md`, `docs/project-management/CHANGE_LOG.md`, `CONTEXT.md`.
- Validation: same constraint as v2.32 — **not compile-verified**, no MetaEditor in this environment. Braces balance on both files (main EA 265/265) and the written unbraced-control-flow detector reports zero suspect sites across the whole `mql5/` tree, including the new script. Every formula and reused function in the script (`B100FreezePrice`, `B100ExecAccountOk`, `B100DemoCancelTicket`, `B100IsDemoAccount`/`B100IsRealAccount`) is cited against its existing, already-shipped line in `DemoExec.mqh`/`Mode.mqh`; the only genuinely new code is the placement wrapper (magic substituted) and the poll loop. Manual demo-account test procedure is in the script's own header comment. Python suite untouched by this change (still 107 passing as of the v2.32 entry).
- Result: order placement and detection are now architecturally separate — the EA can run indefinitely as a pure detector/logger, and the script is a deliberate, manual, per-snapshot action. No edge claim changes; Live stays gated off in both files.
- Remaining work: the script does not auto-rearm for a new RES/SUP pair while a previous one is still being monitored — by design, but worth revisiting if the owner wants continuous unattended operation. Gapped-fill SL re-anchoring and Telegram alerts (both straightforward, both reuse existing self-contained code) were deliberately left out of this pass. The six audit findings listed in the v2.32 entry above are still open.

### 2026-09-04 — Live accounts enabled in the order script (D-007)

- Request: owner asked to remove the stop-order function from the EA (already done in v2.33) and "fully enable at scripts for real account. I allow it. I want the order trigger by scripts", then chose *any real account* over pinning to one login.
- Changes: `RES-SUP OCO.mq5` to v1.01. `InpAllowLiveTrading` now defaults to `true` and `InpLiveAccountLogin` is removed, so the script trades whatever real account it is launched on. **Bug found and fixed while wiring this up:** the sibling cancel routed through `B100DemoCancelTicket`, which gates on `B100ExecAccountOk()` and therefore on DemoExec.mqh's `g_b100_exec_live_ok` — a global the EA sets from its chart arm button and that a Script never sets. On a real account every cancel would have returned `ACCOUNT_NOT_PERMITTED`, so the first live fill would have left the losing side's stop resting on the broker as an unintended second position. Replaced with a self-contained `CancelTicket()` carrying no hidden global dependency; the fill path now retries the sibling cancel up to 10 times and prints an explicit cancel-by-hand warning if it still survives, instead of reporting success unconditionally. A live run prints the account number and lot size before sending. README's top-line claims ("live orders gated, off by default", "LIVE is rejected in the source", the Modes table) were corrected — they had become materially false once the script could trade live.
- Files affected: `mql5/Scripts/RES-SUP OCO.mq5`, `README.md`, `docs/project-management/DECISION_LOG.md` (D-006, D-007), `docs/project-management/CHANGE_LOG.md`, `CONTEXT.md`.
- Validation: **not compile-verified** — still no MetaEditor here. Braces and parens balance; the unbraced-control-flow detector reports zero sites across the `mql5/` tree. Python suite untouched (107 passing). The owner was shown, before the change was made, that `research/BACKTEST_RESULTS.md` measures this strategy at −4.90% ROI / −0.164R with a best-of-20 t=+0.12 against the ~2.45 bar, and that the code has never been compiled; both are recorded in D-007.
- Result: this is a deliberate, recorded weakening of the fail-closed default that D-001/D-005 established. The remaining guards are that a Script only runs on a deliberate launch, its input dialog shows `InpAllowLiveTrading` on every run, and a live run announces the account and lot size in the log.
- Remaining work: compile in MetaEditor and run one full demo fill+cancel cycle before any live use. The six capture audit findings from the 2026-09-03 entry are still open, as is the EA's now-inert OCO ticket machinery and the CHANNEL-strategy `B100DemoSend` path at line 716 (unreachable on BOX_M30, deliberately left).

