BREAK100 v2.00 — START HERE
===========================
Compile 2.00. Attach M30 only. After stop fill: dotted SL/TP lines with prices on the line (not a Fib object). Telegram M30 only. Live locked.

Seamless train data (PC + Hugging Face stay the same)
  1. Copy BREAK100_hf.txt.example -> Common\\Files\\BREAK100_hf.txt
     token=hf_...
     dataset=yourname/break100-boom
  2. Run tools\\Install-BREAK100-HF-Sync.ps1
     (every 15 min: merge train rows, retrain, push/pull policy)
  3. EA writes a kick file on every closed episode so sync is not stale
  4. Phone Space still works — next PC sync merges those rows in

Same policy.csv the EA already loads. Need 16+ quality=1 rows.
Not a profit claim.
